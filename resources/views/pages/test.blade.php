@extends('layout.master')

@section('content')
    {{-- @foreach ($sections as $section)
        @foreach ($section->divisions as $item)
            {{ $item->type }}
            <br>
        @endforeach
    @endforeach --}}

    {{-- @foreach ($divisions as $division)
        {{ $division->buttons->button_content }}
    @endforeach --}}
@endsection

